name: ClaudeUsage
options:
  deploymentTarget:
    macOS: "14.0"
targets:
  ClaudeUsage:
    type: application
    platform: macOS
    sources:
      - Sources/ClaudeUsage
      - AppIcon.icns
    settings:
      PRODUCT_NAME: Claude Usage
      PRODUCT_BUNDLE_IDENTIFIER: com.lodev09.claude-usage
      MARKETING_VERSION: 1.3.2
      INFOPLIST_FILE: Info.plist
      CODE_SIGN_IDENTITY: "-"
      SWIFT_VERSION: "5.0"
    scheme:
      testTargets: []
